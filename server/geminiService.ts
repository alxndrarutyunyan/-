import { GoogleGenAI } from "@google/genai";
import { TEACHING_DATA } from "../src/data/teachingInfo";

let aiClient: GoogleGenAI | null = null;

function getAiClient(): GoogleGenAI | null {
  if (!aiClient) {
    const apiKey = process.env.GEMINI_API_KEY;
    if (apiKey && apiKey !== "MY_GEMINI_API_KEY") {
      aiClient = new GoogleGenAI({
        apiKey,
        httpOptions: {
          headers: {
            "User-Agent": "aistudio-build",
          },
        },
      });
    }
  }
  return aiClient;
}

const SYSTEM_INSTRUCTION = `
Ты — вежливый, дружелюбный и профессиональный виртуальный ассистент репетитора по английскому и французскому языкам Александра А.
Твоя задача — консультировать потенциальных и текущих учеников строго на основе предоставленных данных о занятиях.

Информация о репетиторе и занятиях:
- Александр А., репетитор с 13-летним опытом, более 120 отзывов на Профи.ру.
- Языки: Английский и Французский язык.
- Целевой уровень учеников: от A2+ (Pre-Intermediate) до B1 (Intermediate) и выше.
- Платформа: Яндекс Телемост с демонстрацией экрана. Установка программ не требуется, включать камеру ученику не обязательно.
- Методика: Максимум практики и самостоятельной работы (70% успеха). Разбор сложных моментов, выправление ошибок, постановка разговорной речи.
- Стоимость первого/пробного занятия: 2 300 ₽.
- Прайс абонементов на 1 месяц (уроки 60 / 90 / 120 минут):
  * 4 занятия (1 раз/нед): 9 000 ₽ / 12 000 ₽ / 15 000 ₽
  * 8 занятий (2 раза/нед): 15 000 ₽ / 18 000 ₽ / 20 000 ₽
  * 12 занятий (3 раза/нед): 20 000 ₽ / 25 000 ₽ / 28 000 ₽
  * 16 занятий (4 раза/нед): 23 000 ₽ / 31 000 ₽ / 35 000 ₽
  * 20 занятий (5 раз/нед): 27 000 ₽ / 37 000 ₽ / 41 000 ₽
- Скидки на долгосрочное обучение:
  * 3 месяца: -15% от прайса
  * 6 месяцев: -25% от прайса
  * 12 месяцев: -40% от прайса
- Правила пропуска: При предупреждении заранее переносим урок на этой же неделе.
- Отпуск: Можно заморозить абонемент, занятия не сгорают.
- Оплата: СБП по номеру +7 (910) 040-35-35 (Александр А., Банк ВТБ). Чек отправлять в Telegram @alxndr_artnn.

Правила твоих ответов:
1. Отвечай коротко, четко, доброжелательно и по делу (Telegram-стиль, с эмодзи).
2. Оформляй списки красиво и понятно.
3. В конце каждого ответа предлагай сделать следующий шаг (например, рассчитать стоимость в калькуляторе или записаться напрямую у Александра @alxndr_artnn).
4. Если пользователь спрашивает то, чего нет в правилах, вежливо предложи написать лично Александру (@alxndr_artnn).
`;

export async function askGeminiAssistant(userQuery: string): Promise<string> {
  try {
    const ai = getAiClient();
    if (!ai) {
      return getFallbackResponse(userQuery);
    }

    const response = await ai.models.generateContent({
      model: "gemini-3.6-flash",
      contents: userQuery,
      config: {
        systemInstruction: SYSTEM_INSTRUCTION,
        temperature: 0.4,
      },
    });

    if (response.text && response.text.trim().length > 0) {
      return response.text;
    }

    return getFallbackResponse(userQuery);
  } catch (error) {
    console.error("Gemini API Error:", error);
    return getFallbackResponse(userQuery);
  }
}

function getFallbackResponse(userQuery: string): string {
  const q = userQuery.toLowerCase();
  if (q.includes("пробн") || q.includes("разов")) {
    return `🎯 *Пробное / разовое занятие:*
Вы можете оплатить первое занятие отдельно — **2 300 ₽**, и только потом принять решение об абонементе.

Оплата по СБП: +7 (910) 040-35-35 (Александр А., ВТБ)
Отправьте чек Александру: @alxndr_artnn`;
  }
  if (q.includes("скидк") || q.includes("акци") || q.includes("долгосрочн")) {
    return `🎁 *Выгодные условия при долгосрочном обучении:*
• **-15%** — при покупке на 3 месяца
• **-25%** — при покупке на 6 месяцев
• **-40%** — при покупке на 12 месяцев

Используйте кнопку «🧮 Калькулятор скидок», чтобы узнать точную стоимость!`;
  }
  if (q.includes("где") || q.includes("платформ") || q.includes("как проходит")) {
    return `💻 *Формат занятий:*
• Занятия проходят в **Яндекс Телемост** с демонстрацией экрана.
• Установка программ не требуется.
• Включать камеру не обязательно — главное комфорт.
• Формат: максимум практики и разбор ваших вопросов.`;
  }
  if (q.includes("оплат") || q.includes("чек") || q.includes("реквизит")) {
    return `💳 *Реквизиты для оплаты (СБП):*
📱 Номер: **+7 (910) 040-35-35**
👤 Получатель: **Александр А.**
🏦 Банк: **ВТБ**

После оплаты отправьте чек и желаемое время в Telegram: @alxndr_artnn`;
  }
  
  return `Здравствуйте! Я виртуальный ассистент Александра.

Вы можете:
• Узнать цены на абонементы по кнопке «💰 Стоимость»
• Рассчитать индивидуальную скидку через «🧮 Калькулятор»
• Ознакомиться с «⚙️ Правилами обучения»
• Или написать Александру напрямую: @alxndr_artnn`;
}
